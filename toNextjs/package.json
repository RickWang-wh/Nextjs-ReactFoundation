{
  "comments":{
    "comment": "If you want to try this project localy, please empty content in this file, include me :(",
    "comment": "Before deleting all, copy commands below:",
    "command": "npm install react@latest react-dom@latest next@latest",
    "command": "copy the 'scripts' section below, and then paste it where it is now."
    "then": "run the 'run npm dev' et viola."
  },
  "scripts": {
    "dev": "next dev"
  },

  "dependencies": {
    "next": "^14.2.4",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  }
}
